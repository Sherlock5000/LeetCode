class Solution:
    def arrayPairSum(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        nums.sort()
        return sum(nums[::2])
    
    
        #A = []
        #for i in range(0, len(nums) - 1, 2):
            #A.append([nums[i], nums[i + 1]])
        #s = 0
        #for j in range(len(A)):
            #s = s + min(A[j])
        #return s
